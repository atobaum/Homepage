extends layout

block head2
    title #{title} - Wiki

block content
    .ui.container.segment
        h1= title
        p 해당 페이지 없음.
        a(href="/wiki/edit/" + encodeURI(title) + '?new=1') 문서 만들기
